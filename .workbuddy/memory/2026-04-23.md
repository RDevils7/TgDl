## 2026-04-23

- 打包 Docker 部署文件为 `tg-dl-docker.tar.gz`（~59KB），包含 Dockerfile、docker-compose.yml、server.js、public/ 等，供用户在 NAS/服务器上构建镜像。本地无 Docker 环境，无法直接 build。
- 用户实际需求是发布到 Docker Hub 的镜像文件。创建了 `.github/workflows/docker-publish.yml` GitHub Actions 工作流：多架构（amd64/arm64）自动构建+推送 Docker Hub，镜像名为 `RDevils7/tg-dl`。
- Git push 需设置 `http.sslBackend openssl` + 代理 `127.0.0.1:7897` 才能成功。
- 用户需在 GitHub Secrets 中配置 DOCKERHUB_USERNAME 和 DOCKERHUB_TOKEN 才能完成推送。


### 修复 TTY 下载进度不显示的问题（已提交 GitHub a5a4388）
- **根因**：`updateStatus()` 引用 `$('#statusText')` 但 HTML 中**不存在该元素**，导致 `resetProgressUI()` 中 JS 抛出 TypeError 崩溃
- **修复1**（index.html）：添加缺失的 `#statusText` 和 `#elapsedTime`
- **修复2**（app.js）：`updateStatus()` / `startTimer()` 添加 null 防御
- **修复3**（server.js）：`addLog()` 同步 ttyOutput；tdl 退出追加 stdout；空下载警告

### Docker NAS 部署支持（已提交 GitHub 26655ac → 5ab907f）
- Dockerfile: 基于 debian:bookworm-slim，多阶段构建，自动安装 Node.js 20 + **iyear/tdl**（Go 项目）
- **关键修正**：tdl 正确仓库是 iyear/tdl（非 tyler1234），版本 v0.18.5
- **多架构支持**：自动识别 amd64/arm64，下载对应 tdl Linux 二进制
- docker-compose.yml：三卷持久化 + TZ=Asia/Shanghai + 代理模板 + 资源限制
- server.js: TDL_PATH/DOWNLOAD_DIR/CONFIG_FILE 支持环境变量（兼容 Windows/Linux/Docker）
- README 补充 NAS 专属部署文档（群晖/QNAP/UNRAID）+ 绑定挂载方式
- git push 必须走代理：http.proxy=http://127.0.0.1:7897
