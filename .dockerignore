# 依赖
node_modules/

# 构建产物 / 临时文件
downloads/
data/*.json
!data/.gitkeep

# 开发文件
.workbuddy/
*.log
.DS_Store

# Git
.git/
.gitignore

# 杂项（工作目录中的异常文件）
300
console.error(e))
console.log(a.name
{f.close()}

# 打包文件（不需要进镜像）
tg-dl-docker.tar.gz

# README（镜像不需要）
README.md
